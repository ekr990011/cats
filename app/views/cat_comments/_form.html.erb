<%= form_for(@cat_comment) do |form| %>
  <div class="field">
    <%= form.rich_text_area :body %>

    <%= form.hidden_field :cat_id, value: @cat.id %>

    <div class="article__comment_submit">
      <%= form.submit "Submit", class: "btn submit" %>
    </div>
  </div>
<% end %>
